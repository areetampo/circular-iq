// DEPRECATED — kept as a compatibility shim during modal→drawer migration
// Redirect to the new `useDrawer` hook so older imports don't break until cleanup.
export { default } from './useDrawer';
